export default function ForgetPassword({
    children,
}: {
    children:React.ReactNode;
}) {
    return (
        <div>
            <h1>ForgetPassword</h1>
            {children}
        </div>
    )
}